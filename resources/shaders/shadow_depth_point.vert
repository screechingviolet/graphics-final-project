// #version 330 core
// layout(location = 0) in vec3 aPos;

// uniform mat4 model;
// uniform mat4 view;       // view for current cubemap face
// uniform mat4 proj;       // projection for cubemap faces

// out vec4 vWorldPos;

// void main() {
//     vWorldPos = model * vec4(aPos, 1.0);
//     gl_Position = proj * view * vWorldPos;
// }
